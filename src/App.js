import React, { Component } from 'react';
import './App.scss';

const  App = () => (
    <React.Fragment>
        <Switch>
            <Route exact path='/' component={} />
            <Route  path='/' component={} />
            <Route  path='/' component={} />
        </Switch>
    </React.Fragment>
);
export default App;